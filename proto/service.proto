syntax = "proto3";

package messenger;

option go_package = "proto/";

service Messenger {
    rpc Connect(Request) returns (Response) {}
}

message Response {
    bytes data = 1;
}

message Request {
    bytes data = 2;
}