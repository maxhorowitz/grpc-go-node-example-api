syntax = "proto3";

package messenger;

option go_package = "proto/";

service Messenger {
    rpc Connect(stream Req) returns (stream Res) {}
}

message Res {
    bytes data = 1;
}

message Req {
    int32 id = 1;
    bytes data = 2;
}