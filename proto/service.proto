syntax = "proto3";

package registry;

option go_package = "proto/";

service Registry {
    rpc GetLast(FirstName) returns (LastName) {}
}

message FirstName {
    bytes name = 1;
}

message LastName {
    bytes name = 1;
}